# tegshigzugder.github.io

tegshigzugder.github.io

```zsh
$ gem install jekyll
$ bundle install
$ bundle exec jekyll serve
$ bundle exec jekyll serve --trace
$ bundle show minima
$ jekyll build
$ jekyll serve
$ jekyll serve --livereload
```
